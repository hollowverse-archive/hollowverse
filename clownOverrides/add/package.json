{
  "scripts": {
    "check-ts": "run-p -c --aggregate-output '*/check-ts'",
    "pre-push": "yarn pretest",
    "pre-commit": "yarn common-check",
    "tslint": "yarn lint-ts"
  },
  "devDependencies": {
    "typescript": "^2.7.2"
  }
}