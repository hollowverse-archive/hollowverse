mutation ChangeUserBanStatus($newValue: Boolean!, $userId: ID!) {
  changeUserBanStatus(input: { newValue: $newValue, userId: $userId }) {
    result {
      state
      ... on ErrorResult {
        errors {
          code
          message
        }
      }
    }
  }
}
