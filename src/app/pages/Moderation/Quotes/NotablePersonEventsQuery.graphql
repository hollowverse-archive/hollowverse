query NotablePersonEvents($after: ID) {
  notablePeopleEvents(first: 10, after: $after) {
    edges {
      cursor
      node {
        id

        notablePerson {
          name
        }

        quote
        isQuoteByNotablePerson
        happenedOn
        reviewStatus
        sourceUrl
        postedAt
        submittedBy {
          name
        }
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
